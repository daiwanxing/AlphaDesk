# A 股沪深京成交额看板 — 技术设计

**日期：** 2026-08-02  
**状态：** 草案（头脑风暴已对齐，待实现计划）  
**文档类型：** 技术设计（HOW）  
**平台：** 腾讯云 CloudBase（HTTP 云函数 + 可选文档缓存）+ 现有 Vite/React 静态前端

---

## 0. 已拍板产品决策

| 项         | 决策                                                                             |
| ---------- | -------------------------------------------------------------------------------- |
| 入口       | **独立页面**（导航与事件时间线并列），不挤首页时间线                             |
| UI 密度    | **标准看板**：沪 / 深 / 京三卡 + 合计 + 盘中口径提示 + 更新时间                  |
| 「较上日」 | 方案 **A**：今日累计 vs **上一交易日全天**成交额（盘中为进度对比，非同时刻）     |
| 市场口径   | 交易软件常见「沪 / 深 / 京」**轻量口径**（见 §3），不做全 A 分页加总             |
| 刷新       | 盘中约 **15s** 自动轮询；**休市不轮询**                                          |
| 上游       | 免费东方财富公开接口（`push2` / `push2his`）                                     |
| 架构       | **CloudBase HTTP 云函数代理**（方案 1）；需要再压上游时再加短 TTL 缓存（方案 2） |
| 用户规模   | 约 2 人；不做 Timer 每 15s 写库（方案 3 已排除）                                 |
| 云存储     | **不需要**                                                                       |

---

## 1. 架构总览

### 1.1 原则

- **读路径：** 前端独立页只调自有 HTTP API，**不**浏览器直连东财（CORS / UA / 风控更可控）。
- **写路径：** 本模块**无 LLM、无任务队列**；可选将「昨收全天」写入文档库作按日缓存。
- **休市零轮询：** 前端在非连续竞价时段停止 `setInterval`；**不**部署为本模块服务的 15s CloudBase Timer。
- **YAGNI：** 第一版不做分时同时刻对比、不做近几日柱状、不做全市场逐票加总。

### 1.2 逻辑流

```text
[页面] /market-turnover（名称可调整）
    → 进入时 GET 一次 get-market-turnover
    → 若处于 A 股连续竞价时段：每 15s 再 GET
    → 离开时段 / 离开页面：清除轮询

[HTTP] get-market-turnover
    → 读昨收缓存（pipeline_meta 或专用小文档）；缺失则拉日 K 并写回
    → 拉东财实时成交额（轻量 ulist / 等价一次请求覆盖三市代表标的）
    → 计算差额、百分比、合计
    → 返回 JSON（含 session 状态、口径说明字段）
```

### 1.3 部署边界

| 组件     | 放哪                                                  | 职责                                       |
| -------- | ----------------------------------------------------- | ------------------------------------------ |
| 前端页   | 静态托管 SPA 新路由                                   | 展示三市 + 合计；盘中 15s 轮询；休市停轮询 |
| HTTP API | CloudBase `get-market-turnover`                       | 代理东财 + 昨收缓存 + 聚合                 |
| 昨收缓存 | 文档库（建议复用 `pipeline_meta` 文档 id 或独立 doc） | 每交易日最多刷新一次昨收全天额             |

网关路径示例：`https://<envId>-<appId>.tcloudbaseapp.com/get-market-turnover`

---

## 2. 产品形态（UI）

### 2.1 布局

- 页标题：如「A股量能」
- 三列卡片：沪市 / 深市 / 京市
  - 今日成交额（格式化：亿 / 万亿）
  - 较上日：差额 + 百分比，增量/缩量用颜色区分
- 合计行：三市之和 + 较上日合计变化
- 固定提示文案：`盘中对比昨收全天 · 非同时刻`
- 元信息：数据更新时间、当前会话状态（盘中 / 午休 / 已收盘 / 休市）

### 2.2 交互

| 状态                          | 行为                                         |
| ----------------------------- | -------------------------------------------- |
| 进入页面                      | 立即请求一次                                 |
| 连续竞价中                    | 15s 自动刷新                                 |
| 午休 / 收盘后 / 周末 / 节假日 | **不**自动刷新；可保留手动刷新               |
| 请求失败                      | 保留上一帧；展示错误；退避后再试（避免狂打） |

### 2.3 交易时段（第一版）

- 时区：**Asia/Shanghai**
- 连续竞价近似：工作日 `09:30–11:30`、`13:00–15:00`
- 节假日：第一版可用「仅工作日」近似；可选后续接交易日历。误在假日轮询时仍应低频且可接受（2 用户），但目标是接日历后停掉。

**明确不包含：** 为量能模块配置 CloudBase Timer 定时写库。

---

## 3. 数据口径

### 3.1 实时（今日累计）

轻量代表标的（与常见软件展示接近，实现时在代码/UI 标注来源）：

| 展示名 | 建议标的                                 | 说明                                                   |
| ------ | ---------------------------------------- | ------------------------------------------------------ |
| 沪市   | 上证指数 `1.000001` 成交额               | 与「沪市」高度接近                                     |
| 深市   | 深证成指或深证综指（实现选定一种并固定） | **非**全深市逐票加总；UI 可写「深市（成指/综指口径）」 |
| 京市   | 北证50 `0.899050` 或京市代表             | 轻量近似；不做全 BJ 分页加总                           |

合计 = 三市数值之和（同一口径下相加）。

### 3.2 较上日（方案 A）

- **基准：** 同一标的上一交易日 **日 K 成交额**（全天）
- **对比：** `今日实时累计 − 昨收全天`、`(今日 − 昨收) / 昨收`
- **盘中解读：** 早盘常显示相对「昨收全天」缩量，属预期行为；文案必须说明

### 3.3 昨收缓存

- Key：交易日日期 + 标的
- 每个交易日每个标的最多从东财拉一次日 K；命中缓存则跳过
- 缓存失效：新交易日开盘后发现「昨日」已变

---

## 4. HTTP API 草图

### 4.1 `GET /get-market-turnover`

响应示例（字段名实现时可微调）：

```json
{
  "ok": true,
  "asOf": "2026-08-02T10:42:00+08:00",
  "session": "continuous",
  "compareMode": "vs_prev_full_day",
  "disclaimer": "盘中对比昨收全天 · 非同时刻",
  "markets": [
    {
      "id": "sh",
      "label": "沪市",
      "source": "上证指数",
      "amount": 1190000000000,
      "prevFullDayAmount": 1106000000000,
      "delta": 84000000000,
      "deltaPct": 0.076
    }
  ],
  "total": {
    "amount": 2890000000000,
    "prevFullDayAmount": 2820000000000,
    "delta": 70000000000,
    "deltaPct": 0.024
  }
}
```

`session` 建议枚举：`pre_open` | `continuous` | `lunch` | `closed` | `holiday`（第一版可不区分 holiday）。

### 4.2 错误

- 上游失败：`ok: false` + `error`；HTTP 可仍 200 或 502（与现有 `get-briefs` 风格对齐）
- 云函数侧：超时与重试有限；**禁止**在单次请求内对东财打分页轰炸

### 4.3 限流与风控（上游）

- 东财为免费公开接口，无正式 SLA；按 IP 可能限流/断连
- 2 用户 × 15s × 约 1 次上游请求/刷新：风险低
- 硬约束：实时路径保持 **O(1) 请求**；昨收走缓存
- 后续若需再压：在云函数对「今日快照」加 10–15s 短 TTL（方案 2）

---

## 5. 前端模块边界

建议新 feature 目录，例如：

- `src/features/market-turnover/` — api、类型、session 工具、页面组件
- 路由：TanStack Router 新 page（如 `/turnover` 或 `/a-share-volume`）
- 根布局导航增加入口，链到事件页与量能页

环境变量：`VITE_MARKET_TURNOVER_URL`（或并入现有 CloudBase HTTP base URL 约定）。

---

## 6. 非目标（第一版）

- 上日同时刻对比（方案 B）
- 全市场个股成交额加总
- 近 N 日柱状 / 完成度条（曾讨论的增强版 C）
- 云存储归档
- 付费行情源
- 为量能服务的 CloudBase Timer

---

## 7. 验收清单

| #   | 标准                                              |
| --- | ------------------------------------------------- |
| 1   | 独立页可打开，展示沪/深/京 + 合计                 |
| 2   | 显示较上日差额与百分比；有「非同时刻」提示        |
| 3   | 连续竞价内约 15s 更新；午休/收盘/周末停止自动轮询 |
| 4   | 无本模块 15s Timer；云侧仅按需 HTTP               |
| 5   | 昨收全天有缓存，同日重复刷新不反复打日 K          |
| 6   | 上游失败时不空白狂刷；有错误态                    |

---

## 8. 修订记录

| 日期       | 变更                                                                                     |
| ---------- | ---------------------------------------------------------------------------------------- |
| 2026-08-02 | 初稿：头脑风暴对齐（独立页、标准看板、较上日全天、15s 盘中轮询、云函数代理、休市停轮询） |
