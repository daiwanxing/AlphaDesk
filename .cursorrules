## Git Commit Message（Generate Commit Message 专用）

当 Cursor 在 Source Control 中自动生成 Git commit message 时，必须严格遵守以下规则。此规则优先级高于默认英文输出。

### 语言

- **必须使用简体中文**撰写 subject（以及可选的 body）。
- 禁止输出英文 subject，除非改动内容本身是无法翻译的专有名词（如包名、API 名）。

### 格式

遵循 Conventional Commits：

```
<type>(<scope>): <subject>
```

- `type`：`feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `chore` | `revert` | `build` | `ci`
- `scope`：按改动范围选择，例如 `ui-base`、`ui-business`、`docs`、`Gantt`、组件名或模块名；可省略
- `subject`：中文，简洁说明「为什么改」，不以句号结尾

### scope 参考（本仓库）

- `ui-base`：packages/ui-base
- `ui-business`：packages/ui-business
- `docs`：app/docs 文档站
- 组件名：如 `Gantt`、`Select` 等

### 示例

- `feat(Gantt): 添加受控模式与任务时间变更回调`
- `fix(ui-base): 修复甘特图表头 sticky 横向滚动抖动`
- `docs: 为组件演示启用 VitePress 样式隔离`
- `chore(ui-business): 同步 ui-base peer 依赖下限`

### 写作要求

- 一行标题即可；仅在改动复杂时补充简短中文 body
- 聚焦本次 staged 变更，不罗列无关历史
- 与仓库既有中文 commit 风格保持一致
